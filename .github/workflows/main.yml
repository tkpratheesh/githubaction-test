name: Building and testing Java with Maven
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4.2.2
      - name: Setup Java JDK
        uses: actions/setup-java@v3.14.1
        with:
          java-version: '11'
          distribution: 'temurin'
          architecture: x64
        
        
    
